package JavaBasics;
import  java.util.*;
public class TypeConversion {
    public static void main(String[] args) {
        Scanner sc = new Scanner (System.in);
        float no = sc.nextInt();
        System.out.print(no);
     // this also called implicit conversion bcs it happens automatically by java
        // also called widening since it store smaller data into larger place
    }
}
